Sprint 6 — Pulir, accesibilidad y preparación para backend Objetivo: Mejorar UX, accesibilidad, extraer puntos de integración para futuro backend (endpoints, modelos). Entregables: Tests simples (funciones de calendario y encuesta) Documentación de API (contratos para endpoints) Plan de migración a Firebase o Node+DB Tareas: Agregar roles y seguridad básica (evitar XSS). Crear archivo docs/api.md con contratos JSON (ej.: POST /profiles, GET /recipes). Revisar accesibilidad (labels, tabindex).